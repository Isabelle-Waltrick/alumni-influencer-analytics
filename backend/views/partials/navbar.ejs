<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">Alumni Bidding</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="nav">
      <ul class="navbar-nav me-auto">
        <% if (typeof role !== 'undefined' && role === 'developer') { %>
          <li class="nav-item"><a class="nav-link" href="/developer">API Keys</a></li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/profile">Profile</a></li>
          <li class="nav-item"><a class="nav-link" href="/bidding">Bidding</a></li>
        <% } %>
        <li class="nav-item"><a class="nav-link" href="/alumni-of-the-day">Alumni of the Day</a></li>
        <li class="nav-item"><a class="nav-link" href="/api-docs" target="_blank">API Docs</a></li>
      </ul>
      <span class="text-secondary small me-3">
        <% if (typeof role !== 'undefined') { %>
          <%= role === 'developer' ? 'Developer' : 'Alumnus' %>
        <% } %>
      </span>
      <button class="btn btn-outline-light btn-sm" id="logoutBtn">Logout</button>
    </div>
  </div>
</nav>
<script>
  document.getElementById('logoutBtn')?.addEventListener('click', async () => {
    await api.post('/api/auth/logout', {});
    window.location.href = '/login';
  });
</script>
